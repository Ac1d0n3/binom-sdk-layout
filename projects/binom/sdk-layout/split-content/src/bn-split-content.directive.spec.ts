import { BnSplitContentDirective } from './bn-split-content.directive';

describe('BnSplitContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BnSplitContentDirective();
    expect(directive).toBeTruthy();
  });
});
