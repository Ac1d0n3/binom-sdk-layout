import { BnLayoutHeaderDirective } from './bn-layout-header.directive';

describe('BnLayoutHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
