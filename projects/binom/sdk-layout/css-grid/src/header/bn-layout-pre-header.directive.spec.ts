import { BnLayoutPreHeaderDirective } from './bn-layout-pre-header.directive';

describe('BnLayoutPreHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutPreHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
