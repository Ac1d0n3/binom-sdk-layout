import { BnLayoutSidebarDirective } from './bn-layout-sidebar.directive';

describe('BnLayoutSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
