import { BnLayoutElementBaseDirective } from './bn-layout-element-base.directive';

describe('BnLayoutElementBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutElementBaseDirective();
    expect(directive).toBeTruthy();
  });
});
