import { BnLayoutFooterDirective } from './bn-layout-footer.directive';

describe('BnLayoutFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutFooterDirective();
    expect(directive).toBeTruthy();
  });
});
