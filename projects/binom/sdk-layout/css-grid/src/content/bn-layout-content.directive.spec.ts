import { BnLayoutContentDirective } from './bn-layout-content.directive';

describe('BnLayoutContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutContentDirective();
    expect(directive).toBeTruthy();
  });
});
