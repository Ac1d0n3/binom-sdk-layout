import { BnLayoutPreContentDirective } from './bn-layout-pre-content.directive';

describe('BnLayoutPreContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutPreContentDirective();
    expect(directive).toBeTruthy();
  });
});
