import { BnLayoutAppWrapperDirective } from './bn-layout-app-wrapper.directive';

describe('BnLayoutAppWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutAppWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
