import { BnLayoutWrapperDirective } from './bn-layout-wrapper.directive';

describe('BnLayoutWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
