import { BnLayoutSubHeaderDirective } from './bn-layout-sub-header.directive';

describe('BnLayoutSubHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLayoutSubHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
