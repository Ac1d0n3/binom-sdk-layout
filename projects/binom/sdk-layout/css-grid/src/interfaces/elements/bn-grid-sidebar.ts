export interface BnGridSidebar {
    width:number;
    inHeader:boolean;
    inFooter:boolean;
    iconSidebar:boolean;
    defWidth:number;
    iconSidebarToggle:boolean;
    createToggle:boolean;
    createToogleOnPhone:boolean;
}
