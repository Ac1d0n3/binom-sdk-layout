import { BnGridBlockDirective } from './bn-grid-block.directive';

describe('BnGridBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new BnGridBlockDirective();
    expect(directive).toBeTruthy();
  });
});
