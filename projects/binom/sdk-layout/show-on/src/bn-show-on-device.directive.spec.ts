import { BnShowOnDeviceDirective } from './bn-show-on-device.directive';

describe('BnShowOnDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new BnShowOnDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
