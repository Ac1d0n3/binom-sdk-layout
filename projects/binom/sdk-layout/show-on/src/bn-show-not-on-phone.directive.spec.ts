import { BnShowNotOnPhoneDirective } from './bn-show-not-on-phone.directive';

describe('BnShowNotOnPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new BnShowNotOnPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
