import { BnShowOnPhoneDirective } from './bn-show-on-phone.directive';

describe('BnShowOnPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new BnShowOnPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
